name: Release module

on:
  push:
    tags:
      - '*'

permissions:
  contents: read

jobs:
  release:
    uses: InvoiceShelf/modules/.github/workflows/module-release.yml@3.4.0
    with:
      channel: stable
    secrets: inherit
